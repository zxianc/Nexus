# DEPRECATED — use `nexus_phone`

This module (`com.nexus.assistant`) was the MVP dialer + AI stack.

**Current app:** [`../nexus_phone/`](../nexus_phone/) (`org.fossify.nexus.phone`), Fossify UI + migrated Nexus AI.

Kept in-tree for reference during migration; do not ship as the primary install.
