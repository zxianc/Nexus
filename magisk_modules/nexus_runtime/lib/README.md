# Place shared libs here AND/OR next to nexus_engine in ../bin/:
#   libonnxruntime.so
#
# service.sh / ai_call Supervisor look in bin/ first ($ORIGIN / LibDir).
# Keeping a copy under lib/ is fine; packaging should put the .so in bin/.
